(function($) {
    "use strict"; // Start of use strict

    // Smooth scrolling using jQuery easing
    $('a.js-scroll-trigger[href*="#"]:not([href="#"])').click(function() {
        if (location.pathname.replace(/^\//, '') == this.pathname.replace(/^\//, '') && location.hostname == this.hostname) {
            var target = $(this.hash);
            target = target.length ? target : $('[name=' + this.hash.slice(1) + ']');
            if (target.length) {
                $('html, body').animate({
                    scrollTop: (target.offset().top - 48)
                }, 1000, "easeInOutExpo");
                return false;
            }
        }
    });

    // Closes responsive menu when a scroll trigger link is clicked
    $('.js-scroll-trigger').click(function() {
        $('.navbar-collapse').collapse('hide');
    });

    // Activate scrollspy to add active class to navbar items on scroll
    $('body').scrollspy({
        target: '#mainNav',
        offset: 54
    });

    // Collapse Navbar
    var navbarCollapse = function() {
        if ($("#mainNav").offset().top > 100) {
            $("#mainNav").addClass("navbar-shrink");
        } else {
            $("#mainNav").removeClass("navbar-shrink");
        }
    };
    // Collapse now if page is not at top
    navbarCollapse();
    // Collapse the navbar when page is scrolled
    $(window).scroll(navbarCollapse);

})(jQuery); // End of use strict

function showCourierContent() {
    var courierContent = document.getElementById("courier-content");
    var courierContentDisplay = window.getComputedStyle(courierContent).display;
    var forwardingContent = document.getElementById("forwarding-content");
    var forwardingContentDisplay = window.getComputedStyle(forwardingContent).display;
    var roadTransportContent = document.getElementById("road-transport-content");
    var roadTransportContentDisplay = window.getComputedStyle(roadTransportContent).display;

    if (forwardingContentDisplay === "block") {
        forwardingContent.style.display = "none";
    }

    if (roadTransportContentDisplay === "block") {
        roadTransportContent.style.display = "none";
    }

    if (courierContentDisplay === "none") {
        courierContent.style.display = "block";
    }
};

function showForwardingContent() {
    var courierContent = document.getElementById("courier-content");
    var courierContentDisplay = window.getComputedStyle(courierContent).display;
    var forwardingContent = document.getElementById("forwarding-content");
    var forwardingContentDisplay = window.getComputedStyle(forwardingContent).display;
    var roadTransportContent = document.getElementById("road-transport-content");
    var roadTransportContentDisplay = window.getComputedStyle(roadTransportContent).display;

    if (courierContentDisplay === "block") {
        courierContent.style.display = "none";
    }

    if (roadTransportContentDisplay === "block") {
        roadTransportContent.style.display = "none";
    }

    if (forwardingContentDisplay === "none") {
        forwardingContent.style.display = "block";
    }
};

function showroadTransportContent() {
    var courierContent = document.getElementById("courier-content");
    var courierContentDisplay = window.getComputedStyle(courierContent).display;
    var forwardingContent = document.getElementById("forwarding-content");
    var forwardingContentDisplay = window.getComputedStyle(forwardingContent).display;
    var roadTransportContent = document.getElementById("road-transport-content");
    var roadTransportContentDisplay = window.getComputedStyle(roadTransportContent).display;

    if (courierContentDisplay === "block") {
        courierContent.style.display = "none";
    }

    if (forwardingContentDisplay === "block") {
        forwardingContent.style.display = "none";
    }

    if (roadTransportContentDisplay === "none") {
        roadTransportContent.style.display = "block";
    }
};